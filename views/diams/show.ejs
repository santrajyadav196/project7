<% layout ('layouts/boilerplate') %> 
 <div class="row">
       <div class="col-6 offset-3">
            <h1><%= diam.title[0].toUpperCase() + diam.title.slice(1) %></h1>
    <ul>
        <li><b>Body: </b> <%= diam.body %></li>
        <li><b>isActive:</b> <%= diam.isActive %></li>
        <li><b>Location:</b> <%= diam.location %></li>
    </ul>
    <div>
        <a class="btn btn-info" href="/diams">Back</a>
        <a class="btn btn-warning" href="/diams/<%=diam._id%>/edit">Edit diams</a>
        <form class="d-inline" action="/diams/<%= diam._id %>?_method=DELETE" method="POST">
        <button class="btn btn-danger d-inline">Detele</button>
        </form>
    </div>
       </div>
   </div>